<nav class="Breadcrumb">
  <ol class="Breadcrumb-list">
    <% (@breadcrumb_items || []).each do |item| %>
      <li class="Breadcrumb-listItem">
        <%= item %>
        <% if item != @breadcrumb_items.last %>
          >
        <% end %>
      </li>
    <% end %>
  </ol>
</nav>
